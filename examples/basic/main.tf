# ---------------------------------------------------------------------------------------------------------------------
# BASIC EXAMPLE
# This example demonstrates the basic usage of the module.
# ---------------------------------------------------------------------------------------------------------------------

module "btp_module" {
  source = "../.."

  # Add your module inputs here. Example:
  # subaccount_name = var.subaccount_name
  # subdomain       = var.subdomain
  # region          = var.region
}
